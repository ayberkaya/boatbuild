# API Configuration
# For local development:
REACT_APP_API_URL=http://localhost:3001/api

# For local network access (testing on same WiFi):
# Replace YOUR_IP with your machine's local IP (run: node get-local-ip.js)
# REACT_APP_API_URL=http://YOUR_IP:3001/api
