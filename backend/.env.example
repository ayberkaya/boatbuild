# Database
DATABASE_URL=postgresql://username:password@localhost:5432/boatbuild_crm
DB_HOST=localhost
DB_PORT=5432
DB_NAME=boatbuild_crm
DB_USER=postgres
DB_PASSWORD=your_password

# JWT
JWT_SECRET=your-super-secure-jwt-secret-key-change-in-production
JWT_EXPIRES_IN=24h

# Server
PORT=3001
NODE_ENV=development
# HOST=0.0.0.0  # Uncomment to explicitly bind to all interfaces (default behavior)
# CORS_ORIGIN=http://localhost:3000,http://192.168.1.100:3000  # Comma-separated origins for local network access

# File Upload
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=10485760
